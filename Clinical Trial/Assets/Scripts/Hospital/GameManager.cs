using UnityEngine;

public class GameManager : MonoBehaviour
{
    [SerializeField] private int funds = 10;
    [SerializeField] private int morale = 20;
    [SerializeField] private int infectionRate = 30;
    [SerializeField] private int comfort = 40;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
